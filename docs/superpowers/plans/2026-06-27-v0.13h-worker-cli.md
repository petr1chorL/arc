# V0.13H Worker CLI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add a command-line entrypoint for the execution queue worker.

**Architecture:** Keep `ExecutionQueueWorker` testable. Add a factory that builds it from `create_app`, then expose `main()` for `python -m app.worker` and `arc-one-worker`.

**Tech Stack:** Python argparse, setuptools console scripts, Pytest.

---

- [x] **Step 1: Add worker factory and CLI**

Files changed: `apps/api/app/worker.py`, `apps/api/pyproject.toml`.

- [x] **Step 2: Add factory test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_worker.py -q`

Actual: 2 backend tests pass.

- [x] **Step 3: Verify module help and document startup**

Run: `cd apps/api; .\.venv\Scripts\python.exe -m app.worker --help`

Actual: command help prints supported arguments.
