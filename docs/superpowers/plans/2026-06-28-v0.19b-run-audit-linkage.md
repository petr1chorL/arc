# V0.19B 运行记录与审计联动实施计划

## 目标

把 V0.19A 的 Trace 链路索引和 V0.18A 的 Workspace 审计日志中心连起来：运行观测详情能跳转到按 Trace ID 过滤后的审计日志，审计日志页面也能手动按 Trace ID 查询。

## 文件范围

- `apps/api/app/routers/workspaces.py`
- `apps/api/tests/test_workspace_access_api.py`
- `src/api/audit.ts`
- `src/api/audit.test.ts`
- `src/pages/AuditLog.tsx`
- `src/pages/AuditLog.test.tsx`
- `src/pages/Observability.tsx`
- `src/pages/Observability.test.tsx`
- `src/index.css`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.19B.md`

## RED

1. 在 `test_workspace_access_api.py` 增加 Trace ID 过滤断言，先运行：
   `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_workspace_access_api.py -q -k "audit_events"`
2. 在 `src/api/audit.test.ts` 增加 `traceId` query 断言。
3. 在 `src/pages/AuditLog.test.tsx` 增加 `?traceId=` 初始化过滤断言。
4. 在 `src/pages/Observability.test.tsx` 增加“查看审计日志”链接断言。
5. 运行 focused 前端测试：
   `npx vitest run src/api/audit.test.ts src/pages/AuditLog.test.tsx src/pages/Observability.test.tsx --reporter verbose --pool=forks --fileParallelism=false --testTimeout 15000`

结果：新增测试先因目标行为缺失失败。

## GREEN

1. 后端 `audit-events` endpoint 新增 `traceId` query 参数，并加入 SQLAlchemy where 条件。
2. 前端 API filter 新增 `traceId`。
3. 审计日志页面新增 Trace ID 输入框，读取 URL 初始值，组合现有过滤。
4. 运行观测详情页 Trace ID chip 新增审计日志 deep link。
5. 补充样式，让 Trace filter 和跳转入口在软 UI 风格下不挤压布局。

## 验证

1. Focused 后端测试通过。
2. Focused 前端测试通过。
3. 前端全量测试通过。
4. 后端全量测试通过。
5. `npm run lint` 通过。
6. `npm run build` 通过。
7. 浏览器打开运行观测详情，确认 Trace ID 区块可跳转审计日志，审计页显示 Trace ID 过滤，控制台无新增 error/warning。

## 完成记录

- [x] RED 已观察到失败：后端 `traceId` 被忽略时返回 2 条；前端缺少审计日志链接和 Trace ID 筛选。
- [x] GREEN 已通过 focused 测试：后端 3 项、前端 3 个文件 13 项通过。
- [x] 全量测试、lint、build 已通过：后端 210 项，前端 33 个文件 133 项，`npm run lint`，`npm run build`。
- [x] 浏览器验收已完成：观测页 Trace 链接跳转审计页，审计页 Trace ID 自动填充，控制台 warning/error 为 0。
- [x] 当前实现说明和验收文档已更新。
