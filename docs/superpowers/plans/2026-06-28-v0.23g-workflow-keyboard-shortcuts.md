# V0.23G 工作流编辑快捷键实施计划

## 范围

为工作流编排页增加键盘快捷键：撤销、重做、删除选中连线，以及带确认地删除选中节点。

## 文件

- `src/pages/Workflows.tsx`
- `src/pages/Workflows.test.tsx`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.23G.md`
- `.scratch/v0.23g-workflow-keyboard-shortcuts/PRD.md`
- `.scratch/v0.23g-workflow-keyboard-shortcuts/issues/01-workflow-keyboard-shortcuts.md`

## 步骤

1. [x] RED：新增 `Ctrl+Z` / `Ctrl+Y` 快捷键测试，确认当前不会响应键盘命令。
2. [x] GREEN：增加页面级 keydown 监听，调用既有撤销/重做逻辑，并跳过输入控件。
3. [x] RED/GREEN：补输入框中快捷键不影响画布的测试与实现。
4. [x] RED/GREEN：补选中连线后 `Delete` / `Backspace` 删除连线，并可撤销恢复。
5. [x] RED/GREEN：补选中节点后 `Delete` / `Backspace` 进入确认提示，取消不删，确认才删。
6. [x] 验证：运行 Workflows 聚焦测试。
7. [x] 回归：运行前端拆分测试、后端 pytest、lint 和 build。
8. [x] 浏览器验收：登录本地 4173，验证撤销/重做快捷键、连线删除快捷键和节点删除确认。
9. [x] 文档：更新 V0.23G 验收文档、当前实现说明和本地 Issue 状态。

## 命令

```powershell
npx vitest run src/pages/Workflows.test.tsx --reporter verbose --pool=forks --fileParallelism=false --testTimeout 15000
npx vitest run src/api src/components src/auth src/domain src/App.test.tsx --pool=forks --fileParallelism=false --testTimeout 15000 --reporter verbose
npx vitest run src/pages/ActivateInvitation.test.tsx src/pages/AgentDetail.test.tsx src/pages/Agents.test.tsx src/pages/AssetLibrary.test.tsx src/pages/AuditLog.test.tsx src/pages/Login.test.tsx src/pages/Members.test.tsx src/pages/ModelProviders.test.tsx --pool=forks --fileParallelism=false --testTimeout 15000 --reporter verbose
npx vitest run src/pages/Evaluations.test.tsx src/pages/Reviews.test.tsx --pool=forks --fileParallelism=false --testTimeout 15000 --reporter verbose
npx vitest run src/pages/Observability.test.tsx src/pages/Runs.test.tsx --pool=forks --fileParallelism=false --testTimeout 15000 --reporter verbose
apps\api\.venv\Scripts\python.exe -m pytest apps/api/tests -q -ra
npm run lint
npm run build
git diff --check
```

## 完成证据

- `src/pages/Workflows.test.tsx`：21 条通过。
- 前端拆分回归：`src/api`、`src/components`、`src/auth`、`src/domain`、App、页面回归均通过。
- 后端 pytest：全量命令在当前 shell 超时，按 `apps/api/tests/test_*.py` 逐文件执行全部通过。
- `npm run lint`：通过，无 warning。
- `npm run build`：通过。
- 浏览器截图：`.scratch/v0.23g-workflow-keyboard-shortcuts/browser-keyboard-shortcuts-acceptance.png`。
