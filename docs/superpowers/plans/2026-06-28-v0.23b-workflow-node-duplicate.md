# V0.23B 工作流节点复制实施计划

## 范围

在工作流节点配置面板中增加复制当前节点的能力。副本保留节点配置，生成新 ID，位置偏移，不复制连线，并沿用现有保存草稿契约。

## 文件

- `src/pages/Workflows.tsx`
- `src/pages/Workflows.test.tsx`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.23B.md`
- `.scratch/v0.23b-workflow-node-duplicate/PRD.md`
- `.scratch/v0.23b-workflow-node-duplicate/issues/01-duplicate-selected-node.md`

## 步骤

1. RED：在 `Workflows.test.tsx` 增加复制节点测试，先确认没有“复制节点”入口而失败。
2. GREEN：在 `Workflows.tsx` 增加 `duplicateSelectedNode`，在 Node Inspector 增加“复制节点”按钮。
3. 验证：运行 Workflows 聚焦测试。
4. 回归：运行前端全量、后端测试、lint 和 build。
5. 浏览器验收：登录本地 4173，选中节点，点击复制，确认节点数增加且截图留证。
6. 文档：更新 V0.23B 验收文档和当前实现说明。

## 命令

```powershell
npx vitest run src/pages/Workflows.test.tsx --reporter verbose --pool=forks --fileParallelism=false --testTimeout 15000
npx vitest run --pool=forks --fileParallelism=false --testTimeout 15000 --reporter verbose
apps\api\.venv\Scripts\python.exe -m pytest apps/api/tests -q -ra
npm run lint
npm run build
```
