# V0.18C 角色变更风险提示实施计划

## 目标

让 Workspace 管理员在成员页修改角色前，直接看到新增/移除权限和高风险能力提示。

## 计划

1. [x] 建立本地 PRD、Issue 与状态文档。
2. [x] 在 `src/pages/Members.test.tsx` 增加升级/降级影响提示 RED 测试。
3. [x] 在 `src/pages/Members.tsx` 基于权限矩阵计算角色差异。
4. [x] 在成员角色选择器下方渲染影响提示与高风险标识。
5. [x] 在 `src/index.css` 补充 Soft UI 风格样式。
6. [x] 跑前端聚焦测试、全量测试、lint、build。
7. [x] 浏览器验收成员页交互。
8. [x] 更新验收文档和当前实现说明。

## 验证

- RED：`npx vitest run src/pages/Members.test.tsx --reporter verbose` 首次失败，页面缺少角色变更影响提示。
- GREEN：`npx vitest run src/pages/Members.test.tsx --reporter verbose` 通过。
- 相关回归：`npx vitest run src/api/members.test.ts src/pages/Members.test.tsx --reporter verbose` 通过。
- 全量前端：`npm run test -- --run` 通过。
- `npm run lint` 通过。
- `npm run build` 通过，保留既有 Vite chunk-size warning。
- 浏览器验收通过，升级/降级提示可见，console warning/error 为 0。

## 非范围

- 不新增后端接口。
- 不实现审批流。
- 不改变角色保存 API。
