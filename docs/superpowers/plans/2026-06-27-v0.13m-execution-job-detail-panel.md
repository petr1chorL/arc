# V0.13M Execution Job Detail Panel Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Show execution job detail and audit events from the Observability queue panel.

**Architecture:** Keep the existing queue operations panel as the entry point. Use the existing `getExecutionJob(workspaceId, jobId)` wrapper from V0.13L. Render an inline detail panel below the queue list so operators can inspect a job without leaving the troubleshooting context.

**Tech Stack:** React, TypeScript, Vitest, Testing Library, native CSS.

---

- [x] **Step 1: Write failing frontend behavior test**

Run: `npm test -- --run src/pages/Observability.test.tsx`

Actual: failed because the queue task card had no accessible `查看详情` button.

- [x] **Step 2: Add detail state and API call**

Files changed: `src/pages/Observability.tsx`.

- [x] **Step 3: Render inline detail panel**

Files changed: `src/pages/Observability.tsx`, `src/index.css`.

Panel shows core job fields, error reason, audit action/outcome, status transition, reason, actor and timestamp.

- [x] **Step 4: Verify focused frontend test**

Run: `npm test -- --run src/pages/Observability.test.tsx`

Actual: 1 test file, 6 tests pass.
