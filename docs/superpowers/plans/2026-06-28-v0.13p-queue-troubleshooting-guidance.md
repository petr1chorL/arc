# V0.13P Queue Troubleshooting Guidance Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Show actionable troubleshooting guidance on execution job detail, derived from the job state that is already available in the queue API response.

**Architecture:** Keep this as a frontend presentation slice. Do not extend backend schema yet. Add a pure helper that maps `ExecutionJob` fields to operator-facing hints, then render those hints inside the inline queue detail panel.

**Tech Stack:** React, TypeScript, Vitest, Testing Library, native CSS.

---

- [x] **Step 1: Write failing Observability UI test**

Run: `npm test -- --run src/pages/Observability.test.tsx -t "shows troubleshooting guidance" --reporter verbose`

Actual: failed because the queue detail panel did not contain `队列排障建议`.

- [x] **Step 2: Add derived troubleshooting hints**

Files changed: `src/pages/Observability.tsx`.

The helper derives hints for dead-letter, running lease, queued backoff, canceled, succeeded, retry count, and explicit error text.

- [x] **Step 3: Render guidance in the queue detail panel**

Files changed: `src/pages/Observability.tsx`, `src/index.css`.

- [x] **Step 4: Verify focused and full gates**

Actual:

- Focused Observability test passed.
- Frontend full tests, lint, build, backend full tests, and browser verification passed.

