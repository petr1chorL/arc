# CI Vitest Worker 隔离实施计划

1. 固化失败条件
   - 增加策略测试，验证 CI 必须使用 8 个独立 Vitest 分片进程。
   - 验证：测试先因现有单进程命令而失败。
2. 最小修改 CI
   - 将前端测试 step 改为顺序执行 `1/8` 至 `8/8` shard，保留单 worker 与文件串行。
   - 验证：策略测试通过。
3. 执行完整门禁
   - 逐分片执行全部前端测试，再运行 lint、部署检查、build 与 diff check。
   - 验证：每项命令均有本轮新证据。
4. 完成对抗式收口
   - 核对没有漏测、重复手工文件清单、产品行为改动或能力夸大。
   - 验证：Issue 验收标准和状态记录完整。
5. 经用户授权后提交并推送
   - 由 GitHub Actions 复跑验证 Linux/Node 24 环境。
   - 验证：PR #35 新一轮 CI 成功。
