# V0.13A Async Execution Queue Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add the first backend slice for async workflow execution without breaking existing synchronous workflow runs.

**Architecture:** Keep synchronous execution as the default path. Add `asyncMode` to run creation, persist queued jobs in `execution_jobs`, and expose a worker endpoint that processes the next queued job for the current workspace.

**Tech Stack:** FastAPI, SQLAlchemy, Pydantic, Pytest.

---

- [x] **Step 1: Write failing async queue test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_async_workflow_run_enqueues_and_worker_processes_next_job -q`

Actual: first run failed because `ExecutionJobRecord` did not exist.

- [x] **Step 2: Add queue model, asyncMode contract, enqueue path, and worker endpoint**

Files changed: `apps/api/app/models.py`, `apps/api/app/schemas.py`, `apps/api/app/execution.py`, `apps/api/app/main.py`, `apps/api/app/migrations.py`.

- [x] **Step 3: Run focused and execution regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_async_workflow_run_enqueues_and_worker_processes_next_job -q`

Actual: 1 focused test passes.

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py apps/api/tests/test_human_workflow_execution.py apps/api/tests/test_human_task_api.py apps/api/tests/test_observability_api.py -q`

Actual: 42 selected tests pass.
