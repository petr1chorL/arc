# Artifact Catalog API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement behavior changes. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expose persisted ArtifactVersion records through a Workspace-scoped catalog API so future frontend pages and governance tools can list concrete Artifact instances.

**Architecture:** Add a read-only Workspace route that joins `ArtifactVersionRecord` to `ArtifactRecord`. Return ArtifactVersion-centric DTOs and support a narrow Data Object Definition filter.

**Tech Stack:** FastAPI, SQLAlchemy, Pydantic, Pytest.

---

## Tasks

- [x] Write a failing API test for `GET /artifacts`.
- [x] Add `ArtifactCatalogItemRead`.
- [x] Import Artifact models into the API router module.
- [x] Add `GET /api/workspaces/{workspaceId}/artifacts`.
- [x] Support `dataObjectDefinitionId` filtering and `limit`.
- [x] Verify Workspace isolation.
- [x] Run focused test, execution API tests and Python compile.

## Evidence

- RED: focused test failed with `404`.
- GREEN: focused test passed after adding the catalog route.
- Regression: execution API tests passed.
