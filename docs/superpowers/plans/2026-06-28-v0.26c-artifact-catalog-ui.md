# Artifact Catalog UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement behavior changes. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make persisted ArtifactVersion instances visible in the product UI.

**Architecture:** Add a thin API client over V0.26B, a Workspace page under `/artifacts`, and a main navigation entry. Keep the page read-only and use the existing soft UI page/panel/card patterns.

**Tech Stack:** React, React Router, TypeScript, Testing Library, Vitest, existing Workspace context and API helpers.

---

## Tasks

- [x] Write failing API client test for `listArtifacts`.
- [x] Write failing page test for Artifact instance rendering and filter behavior.
- [x] Write failing Layout test for the new navigation link.
- [x] Add `ArtifactCatalogItem` type.
- [x] Implement `src/api/artifacts.ts`.
- [x] Implement `src/pages/Artifacts.tsx`.
- [x] Add route and main navigation entry.
- [x] Add responsive page styles.
- [x] Run focused and related frontend tests.

## Evidence

- RED: API test failed because `./artifacts` did not exist.
- RED: page test failed because `./Artifacts` did not exist.
- RED: Layout test failed because no “产出物” link existed.
- GREEN: focused tests passed after implementation.
