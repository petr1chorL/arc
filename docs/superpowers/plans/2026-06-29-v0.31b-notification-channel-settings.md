# V0.31B Notification Channel Settings Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a frontend settings page for Workspace notification channel assets created by V0.31A.

**Architecture:** Add typed frontend API wrappers, a settings page, route/nav wiring, focused tests, and docs. Do not add backend endpoints or real external delivery.

**Tech Stack:** React, TypeScript, React Router, Vitest, Testing Library, CSS.

---

## First-Principles Gate

- Bottom-line objective: channel governance must be visible and operable by Workspace admins.
- Minimal loop: list channels -> create channel -> disable channel.
- This is configuration UI, not delivery UI.

## Adversarial Review Gate

- Do not ask for real secret values; only accept `secretRef` labels.
- Do not add connection tests or delivery actions.
- Do not submit invalid JSON or non-object config.
- Do not keep disable enabled after a channel is disabled.

## File Map

- Modify: `src/types.ts` - add notification channel types.
- Create: `src/api/notificationChannels.ts` and test.
- Create: `src/pages/NotificationChannels.tsx` and test.
- Modify: `src/App.tsx` - route.
- Modify: `src/components/Layout.tsx` and test - nav/title.
- Modify: `src/index.css` - small page styles if needed.
- Modify: `docs/CURRENT_IMPLEMENTATION.md` - V0.31B summary.
- Create: `docs/ACCEPTANCE_V0.31B.md` - verification record.

## Task 1: RED Tests

- [x] **Step 1: API wrapper tests**

Assert list/create/disable call:

```text
GET /api/workspaces/workspace-1/notification-channels
POST /api/workspaces/workspace-1/notification-channels
POST /api/workspaces/workspace-1/notification-channels/channel-1/disable
```

- [x] **Step 2: Page behavior tests**

Assert page loads list, creates a channel, blocks invalid config, and disables an active channel.

- [x] **Step 3: Layout route/nav test**

Assert sidebar links to `/w/ai-capability-center/settings/notification-channels`.

- [x] **Step 4: Verify RED**

Run:

```powershell
npm run test -- src/api/notificationChannels.test.ts src/pages/NotificationChannels.test.tsx src/components/Layout.test.tsx --run
```

Expected: fail because API/page/nav do not exist yet.

Evidence: command failed because `notificationChannels` and `NotificationChannels` modules were missing, and Layout did not expose “通知渠道”.

## Task 2: GREEN Implementation

- [x] **Step 1: Add types and API wrapper**
- [x] **Step 2: Add settings page**
- [x] **Step 3: Wire route and nav**
- [x] **Step 4: Add compact styles**
- [x] **Step 5: Verify GREEN**

Run the same focused test command. Expected: pass.

Evidence: `npm run test -- src/api/notificationChannels.test.ts src/pages/NotificationChannels.test.tsx src/components/Layout.test.tsx --run` passed with 13 tests.

## Task 3: Docs, Final Verification, Commit

- [x] **Step 1: Update docs**
- [x] **Step 2: Run final checks**

```powershell
npm run test -- src/api/notificationChannels.test.ts src/pages/NotificationChannels.test.tsx src/components/Layout.test.tsx --run
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_notification_dispatcher_api.py -q
npm run lint
npm run build
git diff --check
```

Evidence:
- focused frontend test passed with 13 tests.
- backend notification dispatcher/channel API test passed with 17 tests.
- `npm run lint` passed.
- `npm run build` passed with the existing Vite chunk size warning.
- `git diff --check` passed with Windows line-ending warnings only.

- [x] **Step 3: Commit**

```powershell
git add src/types.ts src/api/notificationChannels.ts src/api/notificationChannels.test.ts src/pages/NotificationChannels.tsx src/pages/NotificationChannels.test.tsx src/App.tsx src/components/Layout.tsx src/components/Layout.test.tsx src/index.css docs/CURRENT_IMPLEMENTATION.md docs/ACCEPTANCE_V0.31B.md docs/superpowers/plans/2026-06-29-v0.31b-notification-channel-settings.md
git commit -m "feat: add notification channel settings"
```
