# V0.13B Queue Retry and Dead Letter Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add job-level retry and dead-letter semantics to async workflow execution.

**Architecture:** Keep `execution_jobs` as the durable queue record. A failed worker attempt returns to `queued` until `max_attempts` is reached. The final failed attempt moves to `dead_letter`.

**Tech Stack:** FastAPI, SQLAlchemy, Pydantic, Pytest.

---

- [x] **Step 1: Write failing retry and dead-letter tests**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_async_execution_job_retries_failure_before_dead_letter apps/api/tests/test_execution_api.py::test_async_execution_job_moves_to_dead_letter_after_max_attempts -q`

Actual: first run failed because failed jobs were marked `failed`, and later worker claims returned 404.

- [x] **Step 2: Add job retry state**

Files changed: `apps/api/app/models.py`, `apps/api/app/execution.py`, `apps/api/app/migrations.py`.

Added `max_attempts`, `next_attempt_at`, `dead_lettered_at`, immediate retry selection, and final `dead_letter` status.

- [x] **Step 3: Run focused and execution regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_async_execution_job_retries_failure_before_dead_letter apps/api/tests/test_execution_api.py::test_async_execution_job_moves_to_dead_letter_after_max_attempts -q`

Actual: 2 focused tests pass.

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py apps/api/tests/test_human_workflow_execution.py apps/api/tests/test_human_task_api.py apps/api/tests/test_observability_api.py -q`

Actual: 44 selected tests pass.
