# V0.23C 工作流节点删除保护实施计划

## 范围

在工作流节点配置面板增加删除影响摘要和二次确认。确认删除后沿用现有逻辑移除节点与关联边；取消确认后不改变画布。

## 文件

- `src/pages/Workflows.tsx`
- `src/pages/Workflows.test.tsx`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.23C.md`
- `.scratch/v0.23c-workflow-delete-guard/PRD.md`
- `.scratch/v0.23c-workflow-delete-guard/issues/01-confirm-connected-node-delete.md`

## 步骤

1. RED：在 `Workflows.test.tsx` 增加已连接节点删除保护测试，确认当前会直接删除而失败。
2. GREEN：在 `Workflows.tsx` 计算选中节点的入边、出边、影响连线总数，并传入 Node Inspector。
3. GREEN：Node Inspector 增加删除确认态、取消按钮和确认删除按钮。
4. 验证：运行 Workflows 聚焦测试。
5. 回归：运行前端全量、后端测试、lint 和 build。
6. 浏览器验收：登录本地 4173，打开工作流，选中已连接节点，验证删除影响提示、取消和确认删除。
7. 文档：更新 V0.23C 验收文档、当前实现说明和本地 Issue 状态。

## 命令

```powershell
npx vitest run src/pages/Workflows.test.tsx --reporter verbose --pool=forks --fileParallelism=false --testTimeout 15000
npx vitest run --pool=forks --fileParallelism=false --testTimeout 15000 --reporter verbose
apps\api\.venv\Scripts\python.exe -m pytest apps/api/tests -q -ra
npm run lint
npm run build
git diff --check
```
