# V0.13G Execution Worker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add a resident worker code skeleton for async execution jobs.

**Architecture:** Keep queue processing inside `ExecutionService`. Add a small worker wrapper that owns polling, workspace iteration, and idle looping. This keeps worker behavior testable without spawning a process.

**Tech Stack:** FastAPI app services, SQLAlchemy session factory, Pytest.

---

- [x] **Step 1: Write failing worker test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_worker.py -q`

Actual: first run failed because `app.worker` did not exist.

- [x] **Step 2: Add worker skeleton**

Files changed: `apps/api/app/worker.py`, `apps/api/app/main.py`.

Added `ExecutionQueueWorker.process_once`, `process_until_idle`, `run_forever`, and exposed `execution_service` on app state.

- [x] **Step 3: Run focused verification**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_worker.py -q`

Actual: 1 backend test passes.
