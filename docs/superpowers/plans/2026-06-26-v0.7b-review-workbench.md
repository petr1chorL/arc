# V0.7B Review Workbench Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the Reviews page understandable and verifiable when tasks exist, when no tasks exist, and when filters return no results.

**Architecture:** Keep the existing React/FastAPI boundary. Add front-end aggregation and guidance around existing Human Task APIs without adding demo backend state.

**Tech Stack:** React, TypeScript, Vitest, Testing Library, native CSS, existing FastAPI Human Task API.

---

### Task 1: Acceptance Entry Tests

**Files:**
- Modify: `src/pages/Reviews.test.tsx`

- [x] **Step 1: Write failing tests**

Add tests for guided empty state, review workload metrics, full status filter, and filtered empty guidance.

- [x] **Step 2: Run test to verify it fails**

Run: `npm test -- --run src/pages/Reviews.test.tsx`

Expected: FAIL because the page only shows `暂无人工任务。` and lacks the new metrics/filter behavior.

### Task 2: Reviews UI Implementation

**Files:**
- Modify: `src/pages/Reviews.tsx`
- Modify: `src/index.css`

- [x] **Step 1: Implement the minimal UI**

Add summary metrics, guided empty state, full status options, and filtered empty state.

- [x] **Step 2: Run focused tests**

Run: `npm test -- --run src/pages/Reviews.test.tsx`

Expected: PASS with existing review actions preserved.

### Task 3: Final Verification

**Files:**
- Update: `.scratch/v0.7b-review-workbench/status.md`
- Update: `.scratch/v0.7b-review-workbench/issues/01-review-workbench-acceptance-entry.md`

- [x] **Step 1: Run focused test**

Run: `npm test -- --run src/pages/Reviews.test.tsx`

Expected: PASS.

- [x] **Step 2: Run frontend regression**

Run: `npm test -- --run`

Expected: PASS.

- [x] **Step 3: Run lint and build**

Run: `npm run lint`

Expected: PASS.

Run: `npm run build`

Expected: PASS.
