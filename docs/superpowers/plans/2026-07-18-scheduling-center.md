# V1 Lite 调度中心实施计划

1. 在后端测试中先定义计划 CRUD、权限隔离、Cron/时区校验、幂等派发和重叠跳过行为，确认 RED。
2. 新增调度模型、迁移、Schema 与 service，并把派发接入现有 execution worker，确认聚焦后端 GREEN。
3. 为生产启动脚本增加 Worker 守护与部署测试，确认生产不是假闭环。
4. 在前端测试中先定义侧边栏入口、调度列表、编辑、启停和立即执行行为，确认 RED。
5. 新增调度 API Client、类型、页面、路由和样式，确认聚焦前端 GREEN。
6. 执行相关测试、全量前后端测试、lint、build、浏览器验收、对抗式审查和文档收口。
