# V0.13I Compose Worker Service Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add a Docker Compose service definition for the execution queue worker.

**Architecture:** Keep the API and worker as two services built from the same FastAPI package image. The API service starts `uvicorn`; the worker service starts `python -m app.worker`. Both connect to the Compose `postgres` service through the same `DATABASE_URL`.

**Tech Stack:** Docker Compose, Python slim image, Pytest YAML contract test.

---

- [x] **Step 1: Write failing Compose contract test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_deploy_compose.py -q`

Actual: failed because `api` / `execution-worker` services and `apps/api/Dockerfile` were missing.

- [x] **Step 2: Add Dockerfile and Compose services**

Files changed: `apps/api/Dockerfile`, `compose.yaml`.

- [x] **Step 3: Verify focused test and document acceptance**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_deploy_compose.py -q`

Actual: 2 tests pass.

- [x] **Step 4: Verify Compose config and full regression**

Run: `$env:POSTGRES_PASSWORD='compose_validation_only'; docker compose config`

Actual: config expands API, execution worker, and PostgreSQL services.

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q`; `npm test -- --run`; `npm run lint`; `npm run build`

Actual: backend 183 tests pass; frontend 27 files / 101 tests pass; lint and build pass.

Note: `docker compose build api execution-worker` could not run because Docker Desktop daemon was not available.
