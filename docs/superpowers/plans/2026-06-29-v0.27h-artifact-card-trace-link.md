# V0.27H Artifact 卡片直达运行链路实施计划

## 范围

在 Artifact 列表卡片操作区新增直接进入 Observability 的链接。

## 文件

- `src/pages/Artifacts.tsx`
- `src/pages/Artifacts.test.tsx`
- `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.27H.md`

## 步骤

1. [x] 新增 RED：Artifact 卡片有带 ArtifactVersion ID 的“查看运行链路”链接。
2. [x] 复用现有 `artifactTracePath`，在卡片操作区增加 `Link`。
3. [x] 更新浏览器脚本，覆盖卡片直达链接。
4. [x] 跑 Artifact 相关测试、Observability 回归、lint、build 和浏览器验证。
5. [x] 更新当前实现说明和验收文档。

## 验证命令

```powershell
npm run test -- src/pages/Artifacts.test.tsx -t "card trace" --run
npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run
npm run test -- src/pages/Observability.test.tsx --run
npm run lint
npm run build
```

## 验证结果

- RED：`npm run test -- src/pages/Artifacts.test.tsx -t "cards directly" --run` 首次失败，卡片缺少运行链路入口。
- GREEN focused：同一命令通过，1 项通过。
- Frontend regression：`npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run`，21 项通过。
- Observability regression：`npm run test -- src/pages/Observability.test.tsx --run`，12 项通过。
- Static checks：`npm run lint`、`npm run build` 通过；build 保留既有 chunk size warning。
- Browser：`with_server.py` + `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs` 通过。
