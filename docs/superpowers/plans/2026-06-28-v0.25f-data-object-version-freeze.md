# Data Object Version Freeze Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Freeze the exact Data Object Version snapshot inside published WorkflowVersion snapshots so later Definition edits do not silently change published workflow semantics.

**Architecture:** Keep node draft refs lightweight, then enrich refs during workflow publication. Validation ensures referenced Data Object Versions exist in the current Workspace. Published WorkflowVersion snapshots become the immutable boundary.

**Tech Stack:** FastAPI, SQLAlchemy, Pytest, existing Workflow publication and Data Object Version tables.

---

## Tasks

- [x] Write a failing API test proving WorkflowVersion snapshots do not include Data Object `versionId`.
- [x] Extend workflow validation to check referenced Data Object Version records.
- [x] Enrich workflow publication snapshots with Data Object Version `id` and immutable `snapshot`.
- [x] Preserve old Data Object Version refs after the Definition is edited and republished.
- [x] Run focused test, workflow lifecycle tests, Data Object tests and Python compile.

## Evidence

- RED: focused test failed with `KeyError: 'versionId'`.
- GREEN: focused test passed after workflow publication snapshot enrichment.
- Regression: workflow lifecycle API tests and Data Object Definition API tests passed.
