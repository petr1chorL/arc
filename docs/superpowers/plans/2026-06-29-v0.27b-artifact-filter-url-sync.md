# V0.27B Artifact 筛选 URL 同步实施计划

## 范围

让 Artifact 实例页的 Data Object Definition ID 和 Schema 校验状态筛选进入 URL。

## 文件

- `src/pages/Artifacts.test.tsx`
- `src/pages/Artifacts.tsx`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.27B.md`

## 步骤

1. 新增 RED：初始 URL 带筛选参数时，页面请求和控件值同步。
2. 新增 RED：点击筛选写入 URL；点击清空移除 URL 筛选。
3. 在 `Artifacts.tsx` 从 `useSearchParams` 初始化筛选状态。
4. 在筛选/清空动作里同步 query 参数，并保留其他参数。
5. 跑 Artifact 页面测试、相关回归、lint、build 和浏览器验证。
6. 更新当前实现说明和验收文档。

## 验证命令

```powershell
npm run test -- src/pages/Artifacts.test.tsx --run
npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run
npm run lint
npm run build
```

## 执行记录

- 2026-06-29：先补 RED 测试，确认初始 URL 筛选不会驱动请求，筛选/清空动作也不会同步 query。
- 2026-06-29：在 `Artifacts.tsx` 中用 `useSearchParams` 初始化筛选状态，并在筛选/清空动作中同步 query。
- 2026-06-29：聚焦测试通过：`npm run test -- src/pages/Artifacts.test.tsx -t "artifact filters" --run`，2 passed。
- 2026-06-29：Artifact 页面测试通过：`npm run test -- src/pages/Artifacts.test.tsx --run`，9 passed。
- 2026-06-29：相关前端回归通过：`npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run`，16 passed。
- 2026-06-29：`npm run lint` 通过。
- 2026-06-29：`npm run build` 通过，保留既有 Vite 大 chunk 警告。
- 2026-06-29：浏览器验证通过：筛选 URL 初始化、详情深链接、关闭详情保留筛选、清空移除筛选、重新筛选后打开详情均可用。
