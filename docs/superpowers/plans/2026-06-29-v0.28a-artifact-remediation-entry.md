# V0.28A Artifact 创建修复任务入口实施计划

## 范围

把失败或低分 Artifact 接入现有 Remediation Task 闭环。

## 文件

- `src/pages/Artifacts.tsx`
- `src/pages/Artifacts.test.tsx`
- `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.28A.md`

## 步骤

1. [x] 新增 RED：失败 Artifact 卡片点击“创建修复任务”会 POST Remediation Task。
2. [x] 在 Artifact 页复用 `createRemediationTask`，生成稳定 payload。
3. [x] 增加卡片忙碌、错误和创建成功状态。
4. [x] 更新浏览器脚本，覆盖失败 Artifact 创建修复任务。
5. [x] 跑 Artifact 相关测试、Evaluations API 相关前端测试、lint、build 和浏览器验证。
6. [x] 更新当前实现说明和验收文档。

## 验证命令

```powershell
npm run test -- src/pages/Artifacts.test.tsx -t "remediation" --run
npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run
npm run test -- src/pages/Evaluations.test.tsx -t "remediation" --run
npm run lint
npm run build
```

## 当前验证记录

- RED：`npm run test -- src/pages/Artifacts.test.tsx -t "remediation" --run` 曾因缺少“创建 artifact-version-2 修复任务”按钮失败。
- GREEN：`npm run test -- src/pages/Artifacts.test.tsx -t "remediation" --run` 已通过。
- 回归：`npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run` 已通过。
- 回归：`npm run test -- src/pages/Observability.test.tsx --run` 已通过。
- 回归：`npm run test -- src/pages/Evaluations.test.tsx --run` 已通过。
- 静态检查：`npm run lint` 已通过。
- 构建：`npm run build` 已通过；仅有 Vite chunk size warning。
- 浏览器：`with_server.py` + `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs` 已通过。
