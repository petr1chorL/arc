# V0.13J Queue Backoff Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add exponential backoff before failed execution jobs become claimable again.

**Architecture:** Keep `execution_jobs.next_attempt_at` as the scheduling gate. A failed worker attempt returns to `queued`, but `next_attempt_at` is set to a future timestamp. Existing claim logic already ignores queued jobs whose `next_attempt_at` is in the future.

**Tech Stack:** FastAPI service code, SQLAlchemy model field already present, Pytest.

---

- [x] **Step 1: Write failing retry backoff test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_async_execution_job_retry_uses_future_backoff_before_next_claim -q`

Actual: failed because `next_attempt_at` was set to the current time.

- [x] **Step 2: Implement retry backoff**

Files changed: `apps/api/app/execution.py`.

- [x] **Step 3: Update affected queue tests and verify focused regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py -q`

Actual: 16 tests pass.
