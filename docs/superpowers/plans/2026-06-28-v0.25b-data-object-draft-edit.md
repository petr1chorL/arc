# V0.25B Data Object Draft Edit Implementation Plan

## Goal

Add a backend update endpoint for Data Object Definitions while preserving immutable published snapshots.

## Scope

- [x] Add update request schema with optional `name`, `description`, and `schema`.
- [x] Add `PATCH /data-objects/{definitionId}` endpoint.
- [x] Enforce workspace-scoped name uniqueness on rename.
- [x] Update `updatedAt`.
- [x] Add tests for update, duplicate rename, and old snapshot immutability after republish.
- [x] Document acceptance evidence.

## Out of Scope

- Frontend page.
- Deactivation or deletion.
- Impact analysis.
- Workflow node binding.

## Evidence

- RED: PATCH tests initially returned `404 Not Found`.
- GREEN: `.\.venv\Scripts\python.exe -m pytest tests/test_data_object_definitions_api.py -k "updated or duplicate_name" -q` passed, 2 tests.
- Regression: `.\.venv\Scripts\python.exe -m pytest tests/test_data_object_definitions_api.py -q` passed, 6 tests.
- Syntax: `.\.venv\Scripts\python.exe -m py_compile app\schemas.py app\main.py` passed.
- Frontend gates: `npm run lint` and `npm run build` passed; build keeps the existing Vite large chunk warning.

Acceptance record: `docs/ACCEPTANCE_V0.25B.md`.
