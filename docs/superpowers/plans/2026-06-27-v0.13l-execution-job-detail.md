# V0.13L Execution Job Detail Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add a readable execution job detail API with audit events.

**Architecture:** Keep list responses compact. Add a dedicated detail endpoint that returns the existing `ExecutionJobRead` fields plus operation audit events filtered by `target_type=execution_job` and `target_id=jobId`. Add a frontend API wrapper so the later queue operations page can consume it.

**Tech Stack:** FastAPI, Pydantic response models, SQLAlchemy, Vitest API wrapper test.

---

- [x] **Step 1: Write failing backend detail test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_execution_job_detail_includes_operation_audit_events -q`

Actual: failed because `GET /execution-jobs/{jobId}` returned 404.

- [x] **Step 2: Add detail schemas and route**

Files changed: `apps/api/app/schemas.py`, `apps/api/app/main.py`.

- [x] **Step 3: Write failing frontend wrapper test**

Run: `npm test -- --run src/api/execution.test.ts`

Actual: failed because `getExecutionJob` was not a function.

- [x] **Step 4: Add frontend types and API wrapper**

Files changed: `src/types.ts`, `src/api/execution.ts`.

- [x] **Step 5: Verify focused backend and frontend tests**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py -q`; `npm test -- --run src/api/execution.test.ts`

Actual: backend 17 tests pass; frontend execution API 6 tests pass.
