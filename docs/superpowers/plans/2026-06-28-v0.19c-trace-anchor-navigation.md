# V0.19C Trace 链路定位实施计划

## 目标

让运行观测详情中的 Trace 链路索引从静态摘要变成可定位目录：点击 Span 后跳到对应节点证据，并让当前 Span 高亮。

## 文件范围

- `src/pages/Observability.tsx`
- `src/pages/Observability.test.tsx`
- `src/index.css`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.19C.md`

## RED

1. 在 `Observability.test.tsx` 中 mock `scrollIntoView`。
2. 断言页面出现 `定位 Span span-agent` 按钮。
3. 点击后断言目标节点调用 `scrollIntoView`，并获得高亮 class。
4. 先运行：
   `npx vitest run src/pages/Observability.test.tsx --reporter verbose --pool=forks --fileParallelism=false --testTimeout 15000`

预期：新增测试先因没有定位按钮失败。

## GREEN

1. `RunTroubleshooting` 增加 `activeSpanId` state 和 `scrollToTraceTarget`。
2. `TraceLinkMap` 增加定位按钮、active class 和回调。
3. 节点执行链路 article 增加 `id`、`aria-label` 和 active class。
4. 执行事件流 section 增加 root 锚点。
5. CSS 增加高亮与按钮布局。

## 验证

- [x] Focused Observability 测试。
- [x] 前端全量测试。
- [x] `npm run lint`。
- [x] `npm run build`。
- [x] 浏览器验收点击 Span 定位按钮，确认 URL 不变、页面定位高亮、控制台无 warning/error。

## 完成记录

- [x] RED 已观察到失败：缺少 `定位 Span span-agent` 按钮。
- [x] GREEN 已通过 focused 测试：Observability 11 项通过。
- [x] 全量前端测试通过：33 个文件 134 项。
- [x] lint、build、浏览器验收完成。
