# V0.28B 修复任务深链定位实施计划

## 范围

补齐 Artifact 创建修复任务后的继续处理路径：从 Artifact 成功状态跳转到评估中心，并在 Remediation Tasks 中定位对应任务。

## 文件

- `src/pages/Evaluations.tsx`
- `src/pages/Evaluations.test.tsx`
- `src/pages/Artifacts.tsx`
- `src/pages/Artifacts.test.tsx`
- `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.28B.md`

## 步骤

1. [x] 新增 RED：`/evaluations?taskId=...` 高亮对应 Remediation Task。
2. [x] 新增 RED：Artifact 创建成功后提供“查看修复任务”链接。
3. [x] 在 Evaluations 页读取 `taskId` query，展示定位提示和卡片高亮。
4. [x] 在 Artifact 页成功状态增加当前 Workspace 内的评估中心链接。
5. [x] 更新浏览器脚本，覆盖创建后跳转并定位任务。
6. [x] 跑聚焦测试、相关回归、lint、build 和浏览器验证。
7. [x] 更新当前实现说明和验收文档。

## 验证命令

```powershell
npm run test -- src/pages/Evaluations.test.tsx -t "remediation task deep link" --run
npm run test -- src/pages/Artifacts.test.tsx -t "remediation" --run
npm run test -- src/pages/Evaluations.test.tsx src/pages/Artifacts.test.tsx --run
npm run test -- src/api/artifacts.test.ts src/pages/Observability.test.tsx src/components/Layout.test.tsx --run
npm run lint
npm run build
```

## 验证记录

- RED：`npm run test -- src/pages/Evaluations.test.tsx -t "remediation task deep link" --run` 首次失败，Remediation Tasks 区域不能独立显示并且没有定位提示。
- RED：`npm run test -- src/pages/Artifacts.test.tsx -t "remediation" --run` 首次失败，Artifact 成功状态缺少“查看修复任务”链接。
- GREEN：两个聚焦测试均已通过。
- 回归：`npm run test -- src/pages/Evaluations.test.tsx src/pages/Artifacts.test.tsx --run` 已通过，30 项。
- 回归：`npm run test -- src/api/artifacts.test.ts --run` 已通过，2 项。
- 回归：`npm run test -- src/components/Layout.test.tsx --run` 已通过，6 项。
- 回归：`npm run test -- src/pages/Observability.test.tsx --run` 最终已通过，12 项。
- 静态检查：`npm run lint` 已通过。
- 构建：`npm run build` 已通过；仅有 Vite chunk size warning。
- 浏览器：`with_server.py` + `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs` 已通过。
