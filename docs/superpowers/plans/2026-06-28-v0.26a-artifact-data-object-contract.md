# Artifact Data Object Contract Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement behavior changes. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Persist the Data Object contract on final ArtifactVersion records produced by workflow runs.

**Architecture:** Reuse existing `artifacts` and `artifact_versions`. Add optional Data Object metadata to `ArtifactVersionRecord`; when a workflow run finishes, read the published WorkflowVersion snapshot and copy the nearest output node's frozen `outputDataObjectRef` into the final ArtifactVersion.

**Tech Stack:** FastAPI, SQLAlchemy, SQLite migration helpers, Pytest.

---

## Tasks

- [x] Write a failing execution API test for a workflow with an output Data Object binding.
- [x] Add optional Data Object fields to `ArtifactVersionRecord`.
- [x] Add migration columns for existing SQLite databases.
- [x] Create final ArtifactVersion records from completed workflow runs.
- [x] Copy frozen Data Object Definition ID, Version ID and snapshot from WorkflowVersion.
- [x] Guard Agent direct test runs so they do not read WorkflowVersion snapshots.
- [x] Run focused tests, execution API tests, workflow/Data Object tests, migration tests and Python compile.

## Evidence

- RED: focused test failed because no final `ArtifactVersionRecord` existed.
- GREEN: focused test passed after final ArtifactVersion creation and Data Object snapshot copy.
- Regression: execution API, workflow lifecycle, Data Object and migration tests passed.
