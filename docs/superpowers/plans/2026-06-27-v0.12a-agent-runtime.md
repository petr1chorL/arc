# V0.12A Agent Runtime Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a backend Agent Runtime contract that normalizes Agent input, output, errors, token usage, cost, scoring, attempts, duration, and tool-call placeholders.

**Architecture:** Create `apps/api/app/agent_runtime.py` as a focused module. Keep persistence in `ExecutionService`, and map `AgentRuntimeResult` into the existing `NodeRunRecord` fields.

**Tech Stack:** Python dataclasses, FastAPI service layer, SQLAlchemy records, Pytest.

---

### Task 1: Runtime Contract

**Files:**
- Create: `apps/api/app/agent_runtime.py`
- Create: `apps/api/tests/test_agent_runtime.py`
- Modify: `apps/api/app/execution.py`

- [ ] **Step 1: Write the failing Runtime tests**

Add tests that import `AgentRuntimeExecutor`, `AgentRuntimeRequest`, and assert successful and failed executions return structured results.

- [ ] **Step 2: Run RED**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_agent_runtime.py -q`

Expected: fail because `app.agent_runtime` does not exist.

- [ ] **Step 3: Implement the runtime module**

Create dataclasses for request/result/tool call, implement retry, cost calculation, quality scoring, and sanitized failure.

- [ ] **Step 4: Refactor ExecutionService**

Change `execute_agent` to build an `AgentRuntimeRequest`, call `AgentRuntimeExecutor`, and map the result into `NodeRunRecord`.

- [ ] **Step 5: Run focused verification**

Run:

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_agent_runtime.py apps/api/tests/test_execution_api.py::test_agent_test_run_records_model_usage_and_output apps/api/tests/test_execution_api.py::test_workflow_run_retries_and_persists_node_timeline -q
```

Expected: all selected tests pass.

- [ ] **Step 6: Run backend regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q`

Expected: all backend tests pass.
