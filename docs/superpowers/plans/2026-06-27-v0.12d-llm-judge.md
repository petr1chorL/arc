# V0.12D LLM-as-a-Judge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add the first LLM-as-a-Judge slice with injectable execution, persistent evaluator metadata, ModelGateway JSON parsing, and frontend Rubric configuration.

**Architecture:** Extend Rubric and Evaluation records, add a Judge Gateway contract, route `judgeType=llm` direct evaluations through the gateway, and expose judge type/model controls in the Rubric dialog.

**Tech Stack:** FastAPI, Pydantic, SQLAlchemy, Pytest, React, Testing Library, Vitest.

---

### Task 1: LLM Judge Direct Evaluation

**Files:**
- Create: `apps/api/app/judge_gateway.py`
- Modify: `apps/api/app/models.py`
- Modify: `apps/api/app/schemas.py`
- Modify: `apps/api/app/main.py`
- Modify: `apps/api/app/migrations.py`
- Modify: `apps/api/tests/api_test_support.py`
- Modify: `apps/api/tests/test_evaluations_api.py`

- [x] **Step 1: Write failing test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_evaluations_api.py::test_llm_judge_rubric_evaluation_records_model_and_input_snapshot -q`

Actual: first run failed because `app.judge_gateway` did not exist.

- [x] **Step 2: Implement Judge Gateway contract and persistence**

Added `JudgeGateway`, Rubric judge fields, Evaluation evaluator fields, and SQLite补列.

- [x] **Step 3: Route LLM Rubric evaluation through gateway**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_evaluations_api.py::test_llm_judge_rubric_evaluation_records_model_and_input_snapshot -q`

Actual: after fixing internal snapshot alias compatibility, focused test passes.

- [x] **Step 4: Run evaluation regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_evaluations_api.py -q`

Actual: 14 evaluation tests pass.

- [x] **Step 5: Run full verification**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q`, `npm test -- --run`, `npm run lint`, `npm run build`

Actual: 167 backend tests passed before the ModelJudgeGateway parser slice.

- [x] **Step 6: Add ModelJudgeGateway JSON parser**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_judge_gateway.py -q`

Actual: first run failed because `ModelJudgeGateway` did not exist; after implementation, focused test passes.

- [x] **Step 7: Run Judge gateway regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_judge_gateway.py apps/api/tests/test_evaluations_api.py apps/api/tests/test_model_gateway.py -q`

Actual: 16 selected tests pass.

- [x] **Step 8: Run full verification after ModelJudgeGateway**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q`, `npm test -- --run`, `npm run lint`, `npm run build`

Actual: 168 backend tests pass, 96 frontend tests pass, lint passes, build passes.

- [x] **Step 9: Add frontend Rubric Judge configuration**

Run: `npm test -- --run src/pages/Evaluations.test.tsx`

Actual: first run failed because the Rubric dialog had no `评分器类型` control; after implementation, 14 page tests pass.

- [x] **Step 10: Add Judge schema validation and parse retry**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_judge_gateway.py -q`

Actual: first run failed because `ModelJudgeGateway` did not accept `max_attempts`; after implementation, 3 Judge gateway tests pass.

- [x] **Step 11: Run Judge schema regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_judge_gateway.py apps/api/tests/test_evaluations_api.py apps/api/tests/test_model_gateway.py -q`

Actual: 18 selected tests pass.

- [x] **Step 12: Add Judge Prompt version traceability**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_judge_gateway.py -q`

Actual: first run failed because `judgePromptVersion` was missing; after implementation, input snapshots and system prompts include `llm-judge-v1`.
