# V0.13Q Expired Lease Guidance Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Make expired worker leases visible to operators in the execution queue, and give a clear next-step hint in the queue job detail panel.

**Architecture:** Keep this as a frontend presentation slice. The queue API already exposes `status`, `lockedBy`, `lockedUntil`, and `lastHeartbeatAt`; derive the expired-lease state in `Observability.tsx` without changing backend schema.

**Tech Stack:** React, TypeScript, Vitest, Testing Library, native CSS.

---

- [x] **Step 1: Write failing Observability UI test**

Run: `npx vitest run src/pages/Observability.test.tsx -t "highlights expired worker leases" --reporter verbose`

Actual: failed because a running job with an expired `lockedUntil` did not show `租约已过期`.

- [x] **Step 2: Add derived expired-lease helper**

Files changed: `src/pages/Observability.tsx`.

The helper returns true only for `running` jobs whose `lockedUntil` is present and earlier than the current time.

- [x] **Step 3: Render queue-card risk marker and detail guidance**

Files changed: `src/pages/Observability.tsx`, `src/index.css`.

The queue card shows `租约已过期`, and the detail troubleshooting section shows the operator hint that another Worker can take over.

- [x] **Step 4: Verify focused and full gates**

Run focused UI tests, full frontend tests, lint, build, backend tests, browser verification, and diff check.

Actual:

- Focused Observability test passed.
- Frontend full tests, lint, build, backend full tests, diff check, and browser verification passed.
