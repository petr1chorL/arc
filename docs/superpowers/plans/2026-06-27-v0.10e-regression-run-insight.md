# V0.10E Regression Run Insight Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a lightweight Regression Run quality insight summary that explains trend health and next action.

**Architecture:** Reuse the existing `RegressionRunTrend` derived state in `src/pages/Evaluations.tsx`. Add a pure insight builder that maps recent pass-rate trend into a deterministic status, facts, and recommendation, then render it inside the existing trend panel.

**Tech Stack:** React, TypeScript, Vitest, Testing Library, CSS soft UI.

---

### Task 1: Red Test

**Files:**
- Modify: `src/pages/Evaluations.test.tsx`

- [ ] **Step 1: Add failing test**

Add a test named `shows Regression Run insight for declining risky runs`. Mock three Regression Runs where the latest pass rate is `60`, the previous is `85`, and one Run is below the 70% risk line. Assert that the page shows:

- `Regression Run Insight`
- `质量下滑`
- `最新通过率 60%`
- `较上次 -25`
- `风险 Run 1 个`
- `建议：优先查看最新失败样本`

- [ ] **Step 2: Run focused test**

Run: `npm test -- --run src/pages/Evaluations.test.tsx`

Expected: fail because `Regression Run Insight` does not exist yet.

### Task 2: Implement Insight

**Files:**
- Modify: `src/pages/Evaluations.tsx`
- Modify: `src/index.css`

- [ ] **Step 1: Add types and pure builder**

Add `RegressionRunInsight` and `buildRegressionRunInsight(trend)` beside the trend helper. The builder returns:

- `title`
- `summary`
- `recommendation`
- `tone`
- `latestPassRate`
- `previousDelta`
- `riskRunCount`

- [ ] **Step 2: Render insight card**

Render a `regression-run-insight` region inside the trend section before metric cards.

- [ ] **Step 3: Add styles**

Style the insight card using existing soft UI shadows, restrained status tone, and responsive layout.

- [ ] **Step 4: Run focused test**

Run: `npm test -- --run src/pages/Evaluations.test.tsx`

Expected: all tests in the file pass.

### Task 3: Docs And Verification

**Files:**
- Create: `docs/ACCEPTANCE_V0.10E.md`
- Modify: `docs/CURRENT_IMPLEMENTATION.md`
- Modify: `.scratch/v0.10e-regression-run-insight/status.md`
- Modify: `.scratch/v0.10e-regression-run-insight/issues/01-regression-run-insight-summary.md`

- [ ] **Step 1: Add acceptance doc**

Document how to verify `Regression Run Insight` on the evaluations page.

- [ ] **Step 2: Run full checks**

Run:

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q
npm test -- --run
npm run lint
npm run build
```

Expected: all commands exit 0.

- [ ] **Step 3: Browser validation**

Open `http://127.0.0.1:4173/w/ai-capability-center/evaluations`, verify `.regression-run-insight` exists, has 3 fact cards, and browser console has no error/warning. Save screenshot to `.scratch/v0.10e-regression-run-insight.png`.

- [ ] **Step 4: Commit**

Commit with:

```powershell
git commit -m "feat: summarize regression run insights"
```
