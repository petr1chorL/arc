# V0.17B Asset Audit Panel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Tool / Skill 资产库页面展示每个资产最近的审计流事件。

**Architecture:** 新增 `getToolSkillAssetAuditEvents` API wrapper 和 `ToolSkillAssetAuditEvent` 类型；`AssetLibrary` 在加载资产后并行读取审计事件，失败时静默降级；资产卡片内展示最近 3 条事件。

**Tech Stack:** React, TypeScript, Vitest, Testing Library, existing ARC.ONE soft UI CSS.

---

### Task 1: Frontend RED Test

**Files:**
- Modify: `src/pages/AssetLibrary.test.tsx`

- [x] **Step 1: Add failing test**

Add or extend the load test so it stubs `/asset-library/{assetId}/audit-events` and expects `最近变更` plus an event type to render.

- [x] **Step 2: Run RED**

```powershell
npx vitest run src/pages/AssetLibrary.test.tsx --reporter verbose
```

Expected: FAIL because the page does not call or render audit events yet.

### Task 2: API and Types

**Files:**
- Modify: `src/types.ts`
- Modify: `src/api/assetLibrary.ts`
- Modify: `src/api/assetLibrary.test.ts`

- [x] **Step 1: Add type**

Add `ToolSkillAssetAuditEvent`.

- [x] **Step 2: Add API wrapper**

Add `getToolSkillAssetAuditEvents(workspaceId, assetId)`.

- [x] **Step 3: Add API test**

Verify the wrapper calls `/asset-library/{assetId}/audit-events` and response JSON contains no `apiKey`.

### Task 3: Page UI

**Files:**
- Modify: `src/pages/AssetLibrary.tsx`

- [x] **Step 1: Load audit events per asset**

Add `auditEventsByAssetId` state and a loader similar to impacts.

- [x] **Step 2: Render card section**

Render `最近变更` for assets with audit events, showing event type, outcome, and reason or useful metadata.

### Task 4: Verification and Docs

**Files:**
- Create: `docs/ACCEPTANCE_V0.17B.md`
- Modify: `docs/CURRENT_IMPLEMENTATION.md`

- [x] **Step 1: Run focused tests**

```powershell
npx vitest run src/pages/AssetLibrary.test.tsx src/api/assetLibrary.test.ts --reporter verbose
```

- [x] **Step 2: Run full checks**

```powershell
npm run test -- --run
npm run lint
npm run build
```

- [x] **Step 3: Browser validation**

Open asset library in the local app and confirm recent audit events render without console errors.

### Result

- RED: `npx vitest run src/pages/AssetLibrary.test.tsx src/api/assetLibrary.test.ts --reporter verbose` first failed because the page did not render `最近变更`.
- GREEN focused: same command passed with 2 files and 8 tests.
- Full frontend: `npm run test -- --run` passed with 31 files and 128 tests.
- Lint/build: `npm run lint` passed; `npm run build` passed with the existing Vite chunk-size warning.
- Backend focused: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_tool_skill_assets_api.py::test_tool_skill_asset_audit_events_include_lifecycle_and_runtime_invocations -q` passed.
- Browser: after restarting the API process so V0.17A routes were loaded, `/settings/asset-library` displayed `最近变更` and `tool_skill_asset.create`; page text did not contain `apiKey`; console error count was 0.
