# V0.10F Failure Pattern Summary Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a deterministic failure pattern summary for the latest Regression Run.

**Architecture:** Reuse `filteredRegressionRuns` in `src/pages/Evaluations.tsx`. Add a pure helper that selects the latest run, filters failed Evaluation records, groups them by weakest dimension score, and renders the top clusters inside the existing trend panel.

**Tech Stack:** React, TypeScript, Vitest, Testing Library, CSS soft UI.

---

### Task 1: Red Test

**Files:**
- Modify: `src/pages/Evaluations.test.tsx`

- [ ] **Step 1: Add failing test**

Add `shows failed sample clusters for the latest Regression Run`. Mock one latest run with four records: two failed records whose lowest dimension is `Evidence`, one failed record whose lowest dimension is `Actionability`, and one passed record. Assert:

- `Failure Pattern Summary`
- `最新失败样本 3 条`
- `Evidence 偏低`
- `2 samples`
- `Actionability 偏低`
- `1 sample`
- Representative sample IDs are visible.

- [ ] **Step 2: Run focused test**

Run: `npm test -- --run src/pages/Evaluations.test.tsx`

Expected: fail because the summary does not exist.

### Task 2: Implement Summary

**Files:**
- Modify: `src/pages/Evaluations.tsx`
- Modify: `src/index.css`

- [ ] **Step 1: Add pure helper**

Create `buildFailurePatternSummary(runs)` that returns `null` when the latest run has no failed records, otherwise returns total failed count and top clusters.

- [ ] **Step 2: Render summary**

Render a `failure-pattern-summary` region under `Regression Run Insight` and before the trend metrics.

- [ ] **Step 3: Add styles**

Use existing soft UI shadows and compact cluster cards.

- [ ] **Step 4: Run focused test**

Run: `npm test -- --run src/pages/Evaluations.test.tsx`

Expected: all tests in the file pass.

### Task 3: Docs And Verification

**Files:**
- Create: `docs/ACCEPTANCE_V0.10F.md`
- Modify: `docs/CURRENT_IMPLEMENTATION.md`
- Modify: `.scratch/v0.10f-failure-pattern-summary/status.md`
- Modify: `.scratch/v0.10f-failure-pattern-summary/issues/01-failure-pattern-summary.md`

- [ ] **Step 1: Add acceptance doc**

Document how to verify the failure pattern summary.

- [ ] **Step 2: Run full checks**

Run:

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q
npm test -- --run
npm run lint
npm run build
```

Expected: all commands exit 0.

- [ ] **Step 3: Browser validation**

Open `http://127.0.0.1:4173/w/ai-capability-center/evaluations`, verify `.failure-pattern-summary` exists when the latest run has failed records, and save `.scratch/v0.10f-failure-pattern-summary.png`.

- [ ] **Step 4: Commit**

Commit with:

```powershell
git commit -m "feat: summarize regression failure patterns"
```
