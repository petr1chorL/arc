# V0.13F Execution Job Cancel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Let operators cancel an async execution job before it is processed again.

**Architecture:** Add a backend cancel action that marks queue jobs as `canceled`, clears leases, and updates the associated Run to `已取消`. Add a frontend API method and queue-card button.

**Tech Stack:** FastAPI, SQLAlchemy, React, TypeScript, Vitest.

---

- [x] **Step 1: Write failing backend cancel test**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_execution_job_can_be_canceled_before_worker_claims_it -q`

Actual: first run failed with 404 because the cancel route did not exist.

- [x] **Step 2: Add backend cancel action**

Files changed: `apps/api/app/models.py`, `apps/api/app/schemas.py`, `apps/api/app/migrations.py`, `apps/api/app/execution.py`, `apps/api/app/main.py`.

- [x] **Step 3: Add frontend cancel action**

Files changed: `src/types.ts`, `src/api/execution.ts`, `src/api/execution.test.ts`, `src/pages/Observability.tsx`, `src/pages/Observability.test.tsx`, `src/index.css`.

- [x] **Step 4: Run focused verification**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_execution_job_can_be_canceled_before_worker_claims_it -q`

Actual: 1 backend test passes.

Run: `npm test -- --run src/api/execution.test.ts src/pages/Observability.test.tsx`

Actual: 2 frontend test files, 11 tests pass.
