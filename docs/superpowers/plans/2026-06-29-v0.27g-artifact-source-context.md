# V0.27G Artifact 来源上下文实施计划

## 范围

增强 Artifact 目录和详情的信息可读性，展示已有 Run / NodeRun 来源上下文。

## 文件

- `apps/api/app/schemas.py`
- `apps/api/app/main.py`
- `apps/api/tests/test_execution_api.py`
- `src/types.ts`
- `src/pages/Artifacts.tsx`
- `src/pages/Artifacts.test.tsx`
- `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs`
- `docs/CURRENT_IMPLEMENTATION.md`
- `docs/ACCEPTANCE_V0.27G.md`

## 步骤

1. [x] 新增 RED：后端 Artifact 目录返回 workflow / node 来源上下文。
2. [x] 新增 RED：前端 Artifact 卡片与详情展示来源上下文。
3. [x] 扩展 `ArtifactCatalogItemRead` 与 Artifact 查询逻辑，按 workspace 安全读取 Run / NodeRun。
4. [x] 扩展前端类型和 UI，缺失上下文时回退到 ID。
5. [x] 更新浏览器验收脚本，覆盖来源上下文显示。
6. [x] 跑 focused 测试、相关回归、lint、build 和浏览器验证。
7. [x] 更新当前实现说明和验收文档。

## 验证命令

```powershell
apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py -k artifact
npm run test -- src/pages/Artifacts.test.tsx -t "source context" --run
npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run
npm run test -- src/pages/Observability.test.tsx --run
npm run lint
npm run build
```

## 验证结果

- RED：后端缺 `workflowName` 导致 artifact 测试失败；前端列表缺来源上下文导致 source context 测试失败。
- GREEN focused：`apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py -k artifact`，2 项通过。
- GREEN focused：`npm run test -- src/pages/Artifacts.test.tsx -t "source context" --run`，1 项通过。
- Frontend regression：`npm run test -- src/api/artifacts.test.ts src/pages/Artifacts.test.tsx src/components/Layout.test.tsx --run`，20 项通过。
- Observability regression：`npm run test -- src/pages/Observability.test.tsx --run`，12 项通过。
- Static checks：`npm run lint`、`npm run build` 通过；build 保留既有 chunk size warning。
- Browser：`with_server.py` + `.scratch/v0.26c-artifact-catalog-ui/browser-check.mjs` 通过。
