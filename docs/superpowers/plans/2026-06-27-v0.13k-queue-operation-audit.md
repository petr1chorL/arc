# V0.13K Queue Operation Audit Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Audit successful manual execution queue operations.

**Architecture:** Keep queue state transitions inside `ExecutionService`. The API layer captures the authenticated actor, optional reason, before/after state, and durable job/run context, then writes `AuditEventRecord` after a successful queue operation.

**Tech Stack:** FastAPI request body, SQLAlchemy audit event, Pytest.

---

- [x] **Step 1: Write failing audit tests**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_dead_letter_execution_job_can_be_requeued apps/api/tests/test_execution_api.py::test_execution_job_can_be_canceled_before_worker_claims_it -q`

Actual: failed because no successful queue operation audit event was written.

- [x] **Step 2: Add queue operation request and audit writes**

Files changed: `apps/api/app/schemas.py`, `apps/api/app/main.py`.

- [x] **Step 3: Verify focused execution tests**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py -q`

Actual: 16 tests pass.
