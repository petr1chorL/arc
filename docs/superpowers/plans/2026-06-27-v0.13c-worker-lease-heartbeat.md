# V0.13C Worker Lease and Heartbeat Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task.

**Goal:** Add lease-aware job claiming and heartbeat renewal to the async execution queue.

**Architecture:** Keep execution synchronous inside the worker request, but record a durable worker lease before execution. Queued jobs are claimable; running jobs are claimable only when their lease has expired.

**Tech Stack:** FastAPI, SQLAlchemy, Pydantic, Pytest.

---

- [x] **Step 1: Write failing lease and heartbeat tests**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_execution_job_lease_blocks_claim_until_expired apps/api/tests/test_execution_api.py::test_execution_job_heartbeat_extends_active_lease -q`

Actual: first run failed because expired running jobs were not claimable and heartbeat route did not exist.

- [x] **Step 2: Add lease fields and heartbeat route**

Files changed: `apps/api/app/models.py`, `apps/api/app/execution.py`, `apps/api/app/main.py`, `apps/api/app/migrations.py`.

Added `locked_by`, `locked_until`, `last_heartbeat_at`, `workerId` query support, lease-aware selection, and heartbeat renewal.

- [x] **Step 3: Run focused and execution regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py::test_execution_job_lease_blocks_claim_until_expired apps/api/tests/test_execution_api.py::test_execution_job_heartbeat_extends_active_lease -q`

Actual: 2 focused tests pass.

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_execution_api.py apps/api/tests/test_human_workflow_execution.py apps/api/tests/test_human_task_api.py apps/api/tests/test_observability_api.py -q`

Actual: 46 selected tests pass.
