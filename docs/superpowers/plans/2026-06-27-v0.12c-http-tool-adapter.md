# V0.12C HTTP Tool Adapter Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a safe first HTTP tool adapter contract with injected execution and invocation logging.

**Architecture:** Extend Tool / Skill assets with adapter metadata, add a focused tool runtime module, and expose a test invocation endpoint that writes invocation logs.

**Tech Stack:** FastAPI, Pydantic, SQLAlchemy, Pytest.

---

### Task 1: HTTP Tool Test Invocation

**Files:**
- Modify: `apps/api/app/models.py`
- Modify: `apps/api/app/schemas.py`
- Modify: `apps/api/app/main.py`
- Create: `apps/api/app/tool_runtime.py`
- Modify: `apps/api/tests/api_test_support.py`
- Create: `apps/api/tests/test_tool_runtime_api.py`

- [x] **Step 1: Write failing tests**

Cover successful fake HTTP execution, failed execution with sanitized error, and non-HTTP asset rejection.

- [x] **Step 2: Run RED**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_tool_runtime_api.py -q`

Expected: fail because `app.tool_runtime` and endpoint do not exist.

- [x] **Step 3: Implement runtime contract**

Add request/result dataclasses, gateway protocol, disabled default gateway, and executor.

- [x] **Step 4: Implement schema, model fields, and route**

Add adapter metadata fields and `POST /asset-library/{assetId}/test-invocations`.

- [x] **Step 5: Run focused verification**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_tool_runtime_api.py apps/api/tests/test_tool_skill_assets_api.py apps/api/tests/test_tool_skill_invocation_logs_api.py apps/api/tests/test_agent_lifecycle_api.py -q`

Actual: 13 selected tests pass.

- [x] **Step 6: Run regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q`

Actual: 161 backend tests pass before Agent execution invocation context was added.

- [x] **Step 7: Run frontend and build gates**

Run: `npm test -- --run`, `npm run lint`, `npm run build`

Actual: 96 frontend tests pass, lint passes, build passes.

- [x] **Step 8: Add Agent execution invocation context**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_tool_runtime_api.py -q`

Actual: first run failed because Agent execution did not write a Tool invocation log; after implementation, 4 tests pass.

- [x] **Step 9: Run execution-chain regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_tool_runtime_api.py apps/api/tests/test_agent_runtime.py apps/api/tests/test_execution_api.py apps/api/tests/test_agent_lifecycle_api.py apps/api/tests/test_tool_skill_assets_api.py apps/api/tests/test_tool_skill_invocation_logs_api.py -q`

Actual: 23 selected tests pass.

- [x] **Step 13: Run final Trace slice verification**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q`, `npm test -- --run`, `npm run lint`, `npm run build`

Actual: 163 backend tests pass, 96 frontend tests pass, lint passes, build passes.

- [x] **Step 10: Run final full verification**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests -q`, `npm test -- --run`, `npm run lint`, `npm run build`

Actual: 162 backend tests pass, 96 frontend tests pass, lint passes, build passes.

- [x] **Step 11: Add Tool invocation Trace event**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_observability_api.py::test_observability_run_detail_includes_tool_invocation_events -q`

Actual: first run failed because execution events did not include `tool_skill_invocation`; after implementation, the focused test passes.

- [x] **Step 12: Run observability regression**

Run: `apps/api/.venv/Scripts/python.exe -m pytest apps/api/tests/test_observability_api.py apps/api/tests/test_tool_runtime_api.py apps/api/tests/test_execution_api.py -q`

Actual: 23 selected tests pass.
