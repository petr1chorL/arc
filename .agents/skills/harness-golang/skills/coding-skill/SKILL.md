---
name: coding-skill-golang
stage: ② 编码实现
description: 按已确认的需求卡与编码规范，小步实现可编译的 Go 代码
---

# 编码实现技能（coding-skill）— Go 版

> **流水线阶段**: ② 第二步
> **输入**: 已审批的 `.harness/changes/<id>/change.md`
> **出口门禁**: 失败测试先存在且已转绿 · `{{BUILD_CMD}}` 通过 · 符合 `.harness/rules/编码规范.md`

### 核心规则（一句话摘要）

> **先写失败测试 → 最小实现通过 → 测试保护下重构。一次只做一个 Red-Green-Refactor 循环——垂直切片，不批量。**

---

## 1. 职责

你是精通 {{FRAMEWORK_DESC}} 的 AI 程序员。按已审批的规格卡精确实现，核心业务逻辑默认采用 TDD。

---

## 2. 前置检查

1. `change.md` 存在且状态为 `coding`
2. 加载上下文：`.harness/rules/工程结构.md` + `编码规范.md`
3. 缺前置 → 退回 ① harnessing-golang

---

## 3. 工作流程

### Step 1: 构造实现上下文
注入：需求摘要 + AC + 边界情况 + 设计约束 + 契约影响 + 测试策略 + 涉及文件。

### Step 2: 确认实现位置
- 对照 `工程结构.md` 确定服务、包、文件的归属
- 优先复用 common/ 已有公共库

### Step 3: TDD 小循环（核心业务逻辑默认遵循）

> **垂直切片原则**：一次只做一个 Red → Green → Refactor 循环。**不要批量写所有测试再批量写实现**——那样你会测试"想象中"的 API 形状，一旦实现发现 API 不对，一堆测试要推倒重写。每个测试都是**示踪弹**（tracer bullet），它在告诉你上一个决策的对错，然后引导你到下一个决策。

1. 从 `change.md` 提取 AC、边界、降级路径
2. 先写一个失败测试（Red）——只写一个，不批量
3. 写最小实现让这个测试通过（Green）——只写刚好的代码
4. 在测试保护下重构（Refactor）
5. 小步通过 `go vet` 再继续下一个 AC
6. 重复：下一个 AC → 下一个失败测试 → 最小实现 → …

### Step 4: 实时自检（对照编码规范）
- 命名、注释是否合规
- LLM/外部调用是否有超时（context）+重试+限频+降级
- 是否有硬编码密钥、魔法值
- 函数 ≤50 行、文件 ≤800 行、圈复杂度 ≤10
- error 是否被正确处理，不吞不忽略

---

## 4. 约束

- ❌ 禁止跳过需求卡直接写代码
- ❌ 禁止实现 change.md 范围外的功能
- ❌ 禁止修改 public API / 架构而不请示
- ❌ 禁止引入未评审的依赖
- ❌ 禁止先写大段实现再回头补核心业务测试
- ❌ 禁止跳过失败测试直接写核心逻辑
- ❌ 禁止批量写所有测试再写实现（违反垂直切片原则）
- ✅ 每个导出符号有注释
- ✅ 核心业务逻辑遵循 Red → Green → Refactor

---

## 5. 异常处理

### 设计偏差
实现发现需求卡假设不成立 → 停下，描述"假设 X 但实际 Y"，给 2-3 个替代方案，等人类决策。

### 依赖阻塞
外部资源不可用 → 写 Mock/Stub 实现核心逻辑，在 change.md 登记"替换 Mock"任务，继续。

### 领域术语冲突
实现时发现代码中的术语与 `.harness/CONTEXT.md` 不一致，或使用了未定义的领域概念 → 调用 `domain-modeling` 技能：精化术语、更新 CONTEXT.md。

术语敲定后**当场写**进 CONTEXT.md，不批量累积。

### 难以复现的 Bug
如果遇到无法稳定复现的 Bug 或非确定性故障，建议运行 `/diagnosing-bugs` 进行 6 阶段诊断。
诊断结果记录到 `.harness/changes/<id>/diagnosis.md`。

---

## 6. 完成标志

失败测试已先行、核心实现可编译且符合规范后，更新 `change.md` 状态 `coding → testing`，进入 ③ 单测编写。