# 04D 量规与样本治理最终审查

2026-09-05；结论：本地工程进入 ready-for-human。范围是既有候选治理，不是来源生成或评分执行。

## Spec 轴（独立审查）

closure_spec 发现量规 A 的晚历史可进入 B 弹窗/编辑页；追加检查发现晚保存/发布/停用和
创建导航可影响已经切换的路由。对应 deferred 测试取得真实 RED/GREEN，按 Workspace/路由
隔离并保护 await 后回调；候选旧列表覆盖确认状态由主线程 RED/GREEN 修复。
休眠 feedback Function 补齐，并在环境读取/数据库连接之前拒绝直接 URL。
最后只读复核认为限定治理 AC 无仍未修复的已证实严重实现缺口；完整测试由主线程执行，未混算。

## Standards 轴（独立审查）

closure_standards 实际复现 Session→User 与 User→Session 反序，以及会员管理联合锁的反序死锁。
候选确认先取得 User 共享锁再鉴权 Session；成员写入先锁目标 User，并统一 User→Membership 顺序。
未改变全局 authenticate 的默认顺序，限定修改受影响路径。
八个真实 HTTP Handler/PG 场景通过：他人/本人专家降级、资格撤销、成员停用、用户停用。
独立核对实际阻塞、最终状态、每候选一个样本/审计、撤销后重放及随机 schema 清理。
本人停用 User/Membership 继续按既有规则409而非绕过保护。最终未发现需再修的严重锁序缺口。
覆盖确认先持锁/管理随后竞争，不声称穷举全部操作次序；源码/测试所有权与主线程证据分开记录。

## 验证质量与限制

量规PG161、候选PG139、资格管理竞争8场景；全量前端61文件605项、完整浏览器8条通过。
当前Python全量591项是本轮此前Session39319的终态结果，此后未改Python源码。
浏览器确认两版量规阈值保持85/95、停用刷新，专家201/非专家403和刷新候选状态；
未知运行/任务接口不代理旧服务。候选顶部和滚动后确认区截图已查看。
全量首轮604通过/1失败为旧审核页异步成功提示同步断言；改成等待同一提示，聚焦19/全量605通过。
不将定位器错误、偶发测试时间差或旧worker卡顿描述为业务RED或已查明全部根因。

## 尚不包含

远程CI、生产部署/数据对账、真实候选生成、模型评分、完整人工任务/执行/通知、切流与退役Zeabur。
当前历史异常投影和锁测试是明确覆盖的边界，不宣称所有任意坏数据或并发排列均已穷举。
